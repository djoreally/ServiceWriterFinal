import type { InputConfig } from "./utils/input";
import type { OutputConfig, PlaybackEventTarget } from "./utils/output";
import type { BaseConnection, FormatConfig } from "./utils/BaseConnection";
import type { AgentAudioEvent, InterruptionEvent } from "./utils/events";
import { BaseConversation, type Options, type PartialOptions } from "./BaseConversation";
import type { InputController } from "./InputController";
import type { OutputController } from "./OutputController";
export declare class VoiceConversation extends BaseConversation {
    private input;
    private output;
    private playbackEventTarget;
    private cleanUp;
    private wakeLock;
    readonly type = "voice";
    private static requestWakeLock;
    static startSession(options: PartialOptions): Promise<VoiceConversation>;
    private inputFrequencyData?;
    private outputFrequencyData?;
    private visibilityChangeHandler;
    private handlePlaybackEvent;
    protected constructor(options: Options, connection: BaseConnection, input: InputController, output: OutputController, playbackEventTarget: PlaybackEventTarget | null, cleanUp: () => void, wakeLock: WakeLockSentinel | null);
    protected handleEndSession(): Promise<void>;
    protected handleInterruption(event: InterruptionEvent): void;
    protected handleAudio(event: AgentAudioEvent): void;
    private calculateVolume;
    setMicMuted(isMuted: boolean): void;
    getInputByteFrequencyData(): Uint8Array<ArrayBuffer>;
    getOutputByteFrequencyData(): Uint8Array<ArrayBuffer>;
    getInputVolume(): number;
    getOutputVolume(): number;
    changeInputDevice({ sampleRate, format, preferHeadphonesForIosDevices, inputDeviceId, }: FormatConfig & InputConfig): Promise<void>;
    changeOutputDevice({ sampleRate, format, outputDeviceId, }: FormatConfig & OutputConfig): Promise<void>;
    setVolume: ({ volume }: {
        volume: number;
    }) => void;
}
//# sourceMappingURL=VoiceConversation.d.ts.map