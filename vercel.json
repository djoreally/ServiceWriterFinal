{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "npm ci && npm audit fix --package-lock-only && npm audit && (git diff --quiet -- package-lock.json || (git config user.name 'vercel-security-bot' && git config user.email 'security@servicewriter.xyz' && git add package-lock.json && git commit -m 'Apply npm audit lockfile remediation' && git push origin HEAD:main))",
  "buildCommand": "npm run build",
  "crons": [
    {
      "path": "/api/internal/lifecycle/outbox",
      "schedule": "*/5 * * * *"
    },
    {
      "path": "/api/internal/notifications/push/outbox",
      "schedule": "*/5 * * * *"
    }
  ]
}
