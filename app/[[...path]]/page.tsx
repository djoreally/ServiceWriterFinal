/**
 * The preserved UI owns navigation through its capability-aware client router.
 * This catch-all route makes every legacy and new deep link resolvable by
 * Next.js while the client application decides which screen to render.
 */
export default function ServiceWriterRoute() {
  return null;
}
