# Shot 24 — Customer Appointment Management Release Certification

Exact detached head: generated by this commit.

## Scope certified by source inspection
- Digest-only appointment-management token storage.
- Legacy-token digest backfill before plaintext removal.
- Public cancel/reschedule verification by SHA-256 digest.
- Signed-in customer portal no longer receives bearer management tokens.
- Signed-in portal actions bind appointment IDs to authenticated customer ownership.
- Server-side token issuance rotates prior active tokens and stores only digests.
- Replay/use-count, digest-format, and expiry-order constraints.
- Legacy plaintext metadata cleanup.
- Adversarial contract tests and SQL verification plan.
- Legacy MyBookings path migrated to the token-free v2 portal contract.

## Required release order
Apply migrations in timestamp order from 20260911160000 through 20260911230000 only after all earlier detached hardening migrations in this release chain are reviewed and applied in the same environment.

## Certification status
SOURCE CONTRACT: PASS.
DETACHED CHAIN INTEGRITY: PASS.
PRODUCTION DATABASE: NOT MUTATED.
PRODUCTION DEPLOYMENT: NOT TRIGGERED.
FULL CI / TYPECHECK / JEST / NEXT BUILD: NOT CERTIFIED ON THIS DETACHED HEAD.

The repository CI workflow runs automatically only for pushes to main/develop or pull requests targeting those branches. This hardening chain intentionally remains detached, so calling the release fully green before executing CI on the exact head would be inaccurate.

## Promotion gate
Before merging:
1. Run npm ci.
2. Run npm run lint.
3. Run npm run typecheck.
4. Run npm test -- --ci.
5. Run npm run build.
6. Apply migrations in an isolated/staging database.
7. Run supabase/ops/20260911_appointment_management_adversarial_verify.sql.
8. Verify one legacy pre-migration customer link still works after digest backfill.
9. Verify a new confirmation email contains a usable management link while no plaintext token exists in appointments.
10. Only then move main and deploy.
